@model WarehouseManagementMVC.Models.Branch
@{
    ViewBag.Title = "AddBranchItem";
}

<h2>Add Items | @Model.Location</h2>
@using (Html.BeginForm())
{

    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
    <table class="table table-bordered">
        <tr>
            <th>Items Available in the warehouse</th>
            <th>Quantity</th>
        </tr>
        @{List<WarehouseManagementMVC.Models.WItem> itemsIn = ViewBag.WrsItems;}
        @foreach (var item in itemsIn)
        {
            <tr>
                <td>
                    <input type="checkbox" onclick="document.getElementById('@item.Id').disabled = this.checked?false:true;" name="selectedItems" value="@item.Name" id="chk_@item.Id" /> <label for="chk_@item.Id">@item.Name</label>
                </td>
                <td>
                    <input disabled="disabled" type="number" id="@item.Id" class="form-control" style="display:inline-block; width:80px" name="qty" value="" /> <mark>@item.Quantity Maximum</mark>
                </td>
            </tr>
        }
        <tr>
            <td colspan="2">
                <div class="form-group">
                    <div class="col-md-offset-2 col-md-10">
                        <input type="submit" value="Save" class="btn btn-default" />
                    </div>
                </div>
            </td>
        </tr>
    </table>

            }